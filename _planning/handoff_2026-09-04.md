# Handoff — 4 September 2026

If you (or a fresh session) opens this next, here is where we are.

Two commits, `a51ac6a` and `9f41944`, both **deployed, hash-verified (100 of 100
servable files) and pushed** to `origin/site-command-centre`. The home page now
follows node **196:7243**, the designer's "Current Design": a Recent
Observations rail between the announcements and the modules, a sticky search
field that costs the resting page nothing, the announcements band's 10% white-to-teal
fill with its 32px bottom radius, and a module card that finally holds the
design's 1.093 proportion across the tablet band. The deck's swipe and its
release now share one geometry, so a card no longer springs back towards the
finger, and a small reactions library drives like, save, counts and the rail's
entrance. `tools/verify_home.py` and `tools/verify_deck.py` assert all of it
against the node's own numbers and pass at 1024/899/834/810/768 alongside
`tools/verify.py`.

**The one thing worth knowing before you touch anything.** "I can't see the
Modules cards" on an iPad 9 was **not a bug**. The grid starts at y=1281 and
that screen is 1080 tall, so the first card is 195px below the fold in portrait
and 471px below it in landscape — which is exactly what the artboard draws.
Chrome's device emulation hid it because "Fit to window" scales the render
down. Diagnose that class of report by measuring the document, not by looking at
an emulator.

**Two decisions are waiting on the user, and neither should be guessed.**
First, the folder reorganisation: a full plan was presented and approved by
nobody, so nothing was moved. It flagged five things that cannot be done as
specified — `docs/README.md` duplicating the root one, `docs/modules.md` being
41 files rather than one, no `package.json` existing, three `src/` folders with
nothing to hold, and `tools/` being absent from the target. Second, whether to
build **Ask ANTZ** for real. `mockups/interaction-concepts.html` is a working,
pressable mockup of it and five smaller ideas, read off the two screen
recordings the user added to `References/Animetions & App Strcutres/`. It is the
only idea of the six that changes what people can find out.

**Deliberately uncommitted, all of it someone else's call:** the two screen
recordings (107MB) and every mockup, by explicit instruction;
`.claude/settings.json`, which gained a `claude mcp *` permission when the
Agentation server was registered and which would ship to everyone who clones;
`assets/icon.zip`, unclassified; `mockups/device-preview.html`, which came from
another session. The 2 September handoff note has now been raised **six times**
without a ruling. All of these are already excluded from production — `mockups`,
`_planning`, `References` and `tools` are in `.vercelignore`, verified by
request against the live site.

**Still open from before:** Reports sits on the page but is invisible in the
design, where the Modules group is fixed at 947px and clips row five. Security
draws a padlock where Figma draws a shield. `dashboard.html` never inherited
`landCards()`, so its cards still teleport in. And real WebKit remains
unverified — Safari and `safaridriver` are installed but "Allow remote
automation" is off, which is one toggle in Safari's Developer settings and would
let the whole suite run against the engine the iPad actually uses.

---

## Second session, 4 September 2026 — evening

Three commits, deployed and verified live at
`https://antz-module-selection-home.vercel.app`, pushed to
`origin/site-command-centre`. **Reports is off the default home page** on the
user's ruling (`7d466c1`) — the door is `secondary` so it stays in Add Module,
`VERSION` went 7 → 8 so a saved layout drops it, and the grid is four rows,
which meant correcting `verify_home.py`'s 884 to 704 rather than letting it fail
every run. **The home page has a corner FAB** (`2958359`) that summons the mobile
app's own sixteen Quick Actions — `References/IMG_2226.HEIC`, committed — as a
glass dock: 8×2 in landscape, 4×4 in portrait, a clip-path wipe out of the
button, blur without dim. Chosen by the user from three concepts in
`mockups/fab-actions.html`, which is committed with the two that lost.

**The one thing worth knowing before you touch it.** The dock's layer is hidden
by `display:none` in four states (edit, search, drag, Site panel) and **hiding is
not closing** — without the MutationObserver in `QuickActionsDock.js` the button
returns showing a ✕ over nothing. Any new state that hides `.qa` must join that
list.

**Open, and none of it guessable.** No create flows exist, so `onAction` only
logs; the sixteen are ungated because the Site Command Centre's permission model
is keyed on `d.*` domains these home verbs have no mapping to. **Master** uses
the `layers` glyph, not a shield — this sprite has no shield and a padlock in a
create list reads as "lock something"; one word to change if that is wrong. The
same padlock/shield mismatch on the Security card is still open from before.

**Uncommitted by ruling or by absence of one:** `.claude/settings.json`, the
107MB of screen recordings, and the other sessions' mockups. `assets/icon.zip`
was ruled this session — it is in `.vercelignore` now. The 2 September note has
still not been answered; this is the seventh time it has been carried.
