# Handoff — 8 September 2026

If you (or a fresh session) opens this next, here is where we are.

**Everything this session built is UNCOMMITTED and UNDEPLOYED** — ~1,520
insertions in `index.html`, plus `tools/verify_v2.py`, `assets/icon/note-head.svg`,
`assets/icon/note-like.svg` and `assets/img/focus-animal.jpg`. And a peer session
(`module-selection-f1`) opened on this repo while this one was working, so both
of you are holding in-flight edits to the same file: `stat` it twice and read the
diff before any commit or `vercel --prod`, because both ship the working copy.

What shipped: the app now draws **two home pages** — V1 (default) is node
196:7243 as before, V2 (`?v=2`) is node **236:6669**, which drops the
announcement deck and the observation rail, re-parents the gradient band onto the
module grid, and seeds **My Focus Hub** (two variations, ink and plate) and
**Notes**. All three are 2×2 `secondary` cards under two new door-less modules
`focus` and `notes`, and all three auto-advance through their items with the dots
as navigation. The single most important thing to know before touching them: the
dwell is a **CSS animation**, not a timer — `js/lib/carousel.js` explains why, and
pausing, reduced motion and cleanup all fall out of that one choice.

**The next move** is a ruling on committing: this is two sessions' worth of
uncommitted work on a shared file. Ten suites pass (`verify_v2` at seven widths).

## User-visible acceptance, per thing shipped

- **Two home pages.** Open `/index.html` — announcement deck and Recent
  Observations rail as before. Open `/index.html?v=2` — neither, and the page
  starts at the Command Centre banner. If V2 flashes the deck before hiding it,
  the pre-`<body>` stamp script did not run.
- **The three widgets.** On `?v=2`, the last grid row is My Focus Hub (white) and
  Notes. On V1, press `E` → Add Module → My Focus Hub shows both variations to
  place. If they are missing from Add Module, the `focus`/`notes` modules did not
  register.
- **Auto-advance + navigation.** Leave `?v=2` open ~7s: the hub and Notes change
  item on their own. Tap a dot, or click a card and press ←/→, or swipe. If they
  never move, check `prefers-reduced-motion` — the dwell is disabled there by
  design and the dots still work.
- **Animals and enclosures.** Page the hub to item 2: "HOOFSTOCK BARN /
  Hoofstock · Section B / ENC: HB-04", upright not italic, one grey badge.
- **Notes holds five.** Five dots, and the active one is a pill.
- **Section head gone on V2** (8 Sep feedback). `?v=2` has no
  "All Modules / Edit Modules" row; press `E` and it appears with Add Module and
  Done. If Done is missing you are stranded in edit mode — use the avatar menu.
- **Verifiers pass on localhost again.** `python3 tools/verify_home.py
  http://127.0.0.1:8000/` → ALL PASS. It used to say "2 FAILED"; those were the
  Agentation toolbar's connection-refused errors, now discounted in `cdp.py`.

## Still unruled, carried

`.claude/settings.json` is still modified and uncommitted (the `claude mcp *`
permission) — raised every session since 2 September. The 2 September handoff
note has still not been answered. The 107MB of screen recordings, `assets/icon.zip`
and the other sessions' mockups remain deliberately uncommitted.

---

## Second session of 8 September (afternoon)

The peer named above was this session, and the ~1,520 insertions are committed:
`4ebb239` (the V2 page, both Focus Hub compositions, Notes, the version switch),
`9b71560` (a ten-variation Notes & Announcements mockup) and `92e65af` (the
handoff's live link corrected, plus `SESSION_HANDOFF.md`, `docs` and `.env*` into
`.vercelignore` after `/SESSION_HANDOFF.md` and `/docs/framing-…md` were found
answering 200 on the CDN). All three commits are local on `site-command-centre`,
ahead 2 of its remote at the time of that push and nothing pushed since.
`vercel --prod --yes` shipped `4ebb239`; the alias serves it and both suites pass
against it.

Then V2 was rebuilt to **node 270:4176**, which supersedes 236:6669: four columns
of 162x144 (from 700px up — the 744 frame otherwise lands in this stylesheet's
phone band and drew two), the frame's own row order, and the corner FAB replaced
by one centred gradient pill over a blurred wash, opening the sixteen verbs on the
profile menu's material with the page blurred behind. The pill's label collapses to
the glyph alone on scroll, off the sticky search row's existing observer. `VERSION`
went 9 → 10 → 11 across the day; a saved layout is a complete list, not a diff, so
every seed change needs the bump.

**THE ONE THING TO DO NEXT.** All of that 270:4176 work is **uncommitted and
undeployed** — `index.html`, `tools/verify_v2.py`, `assets/icon/qa-actions.svg` and
two new mockups. The `.vercelignore` fix is committed but only takes effect on the
next upload, so `/SESSION_HANDOFF.md` and `/docs/…` **are still public right now**;
one `vercel --prod --yes` clears both that and ships the new V2. Verify against
`https://antz-module-selection-home.vercel.app`, never the per-deployment URL.

**Open with the user.** The Quick Actions surface is unresolved by design: the
4x4 panel in the build was rejected, ten arrangements of it were rejected as "one
idea ten times", and six premise-deleting ideas are waiting for a pick in
`mockups/quick-actions-ideas.html` (1 Scan-to-act and 3 On-the-card are the pair
I recommended). Also offered and unanswered: raising the Notes / My Focus Hub
clamps now that a 2x2 is 421px tall rather than 304, and seeding Parivesh's wide
door — the last card V1 has that V2 does not.

---

## Second session, 8 September 2026 (evening)

The session above closed asking for a commit and a deploy; both have happened.
**`d9f7e78` is committed and live on
`https://antz-module-selection-home.vercel.app`** — and note it carried the
whole 270:4176 pill replacement that had been sitting uncommitted for four
hours, so read `git diff --stat` against HEAD before any future commit rather
than assuming the tree holds only your own work. `.vercelignore` took effect
with the upload: `/SESSION_HANDOFF.md`, `/docs/…`, `/.claude/settings.json`,
`/.env.local`, `/assets/icon.zip` and `/mockups/…` are all 404 now, verified.

**V2 moved on again, to node 280:3521** — the third revision of the day. A new
card, **Key Insights**, takes the 2-wide cell beside Hospital (births against
deaths and transfers, on a teal→navy ramp of its own, under a new `insights`
pseudo-module and a new `headline` layout); **Pharmacy and Lab moved** out of
that cell to the foot beside My Focus Hub. Seventeen cards, `VERSION` 11 → 12.
The frame also draws a **chat disc** beside the pill, which 270:4176 drew and
nobody built, and it redraws the pill **flat `#37BD69`** at a stadium radius
where it had been a green→navy ramp. Its gradient was *sampled*, not
transcribed: Figma reported the fill with a stop at 133.5% and rendering that
literally put the card's top edge 56 levels of 255 wrong.

**The Quick Actions question from the session above is answered, and not from
`quick-actions-ideas.html`.** The user sent a full prose brief instead: keep all
sixteen visible, and make the FAB feel like it is *transforming into* the panel.
So the panel is now laid out at full size and revealed by a `clip-path` that
begins as the pill's measured box and opens outward — it unfolds out of the FAB
rather than appearing over it. The per-cell icon plate is gone (that was what
made one surface read as sixteen tiles), the reveal staggers by row from the
bottom up off each cell's *measured* top edge, and the close is that reversed.
Two things in it are deliberate reversals of earlier instructions, both on the
brief's explicit terms: the backdrop frost is down to a 6% dim, and the panel no
longer borrows the profile menu's material.

**Read `verify-motion-frame-by-frame` in memory before touching that motion.**
Its timings are measured by freezing transitions with `getAnimations()` and
seeking them — sleeping between screenshots reported the panel fully open 50ms
into a 345ms unfold, because each capture costs more than the animation. That
technique is what caught a curve 66% done at a fifth of its travel, a growth
origin 32px beside the FAB, a stray transition firing before the real one, and
the surface fading out with half its close still to run. None of those is
visible to an end-state check.

**THE ONE THING TO DO NEXT.** Nothing is blocking. The branch
`site-command-centre` is **4 commits ahead of origin and unpushed** — pushing
does not deploy (Vercel is not wired to GitHub), so that is purely about
backing the work up. If you want a code change first, `tools/cdp.py` never
deletes its temp Chrome profile and this session left 230 of them, 2.4GB, which
filled the disk and blocked *every* command for a stretch; the fix is
`shutil.rmtree(self.profile, ignore_errors=True)` in `Chrome.quit()`.

**Open with the user, all offered and unanswered.** (1) The brief said "keep the
FAB at the bottom-right" and it is centred at the foot, because that is what
node 280:3521 draws and what the brief's own diagrams show — one rule moves it
if that reading was wrong. (2) Pending Approvals' second line is coral on V1 and
yellow on V2 because the two frames genuinely disagree; it is scoped per page
rather than picked. (3) Still outstanding from the morning: raising the Notes /
My Focus Hub clamps now a 2x2 is 421px tall, and seeding Parivesh's wide door —
the last card V1 has that V2 does not.
