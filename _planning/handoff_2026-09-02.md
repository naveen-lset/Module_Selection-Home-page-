# Handoff — 2 September 2026

If you (or a fresh session) opens this next, here is where we are.

Six commits, `cc8e94c → f178aee`, all local: the two closed peer sessions' landing
animation was verified and committed, the Hospital glyph took back its `m-` prefix,
and then two mockups and one live build. **`mockups/widget-concepts.html`** is 89
widget ideas over 41 compositions, one block of ten cells per module for all
eighteen modules, every figure from `docs/Modules.md Files/`.
**`mockups/site-workspace.html`** is a second, separate mockup — the Site Workspace
redesigned on the Apple-widget brief, glanceable, no Site Health, no CTA inside a
widget, content-driven heights, five role defaults. The two are different SURFACES,
which is the whole point: §28 of that brief puts the detail on module pages and the
summary on the Site page. **Sixteen of the widget concepts are now built into
`index.html`** — six at 1×1, five at 2×1, five 2×2 signatures — with 34 widgets
using them; `VARIANTS` 76 → 110, `LAYOUTS` 21 → 37, seeded page 16 → 34 cards.
The gap they fill was SIZE, not monotony: measured, the catalogue offered `small`
zero times, so every data card was ≥2 columns.

**The single most important next move: nothing since `d0705b9` is deployed.**
Production is four commits and 66KB behind and does not have any of the new
compositions. `vercel deploy --prod --yes` from a clean tree, then diff the sha
against local as before.

**Three rulings are still open from the Site Workspace brief** and I built the
mockup on stated recommendations rather than answers: (1) §15's soft tonal
gradients against the recorded full-saturation Figma palette — my advice is hold
until Figma changes; (2) whether action widgets exist on the Site Workspace at all
— my advice is module pages only, which is why the three action shapes are absent
from that mockup; (3) accepting the §3/§30 deletions of Site Health and the Needs
Attention card, which reverses an earlier brief. Also live: 34 cards is past
`blurBudget` (18) so the landing correctly drops its blur, and the seeded page is
one array if you want it trimmed.

---

## Close-out — 2 September 2026, evening session

Ten commits since the note above, `f178aee → 38c7943`, **all deployed** — the
backlog that note called the most important next move is cleared, and every
deploy since was sha-diffed against local and re-verified against the live URL.

What shipped: **Global Search** became its own full-screen white page (`.gsx`)
with a token field, the `@` type sheet, the design's thirty-tag vocabulary and
two bottom sheets — rebuilt once, because the first version was a desktop
dropdown I had substituted for the page the design draws. Every anchored menu in
the product now shares `--t-menu: 200ms` and grows from its own anchor
(`menuOrigin()`). The **Add Module** sheet is rebuilt to node `55:3326`
(1056×956, 304px rail, labels above the previews, no chevron). A new
**`dashboard.html`** carries the role-aware dashboard — 18 modules with bespoke
visuals, four roles differing in order/span/depth, audited by
`tools/verify_dashboard.py` across nine widths. The **Site header** is edge-to-
edge with a Karnataka locator map, and the **home banner** is the illustrated
estate from node `126:3995`.

**The single most important thing to know before touching a verifier:** they all
share one Chrome debug port. Run them sequentially with
`pkill -f "remote-debugging-port"; sleep 2` between each. Run two at once and you
get failures that move around and look like product bugs — that cost an hour
today and produced a wrong claim in commit `66688dc` which `1e2180e` retracts.

**Two open questions, both content deletions on the Add Module sheet, both mine
to raise and yours to decide:** the rail's four group headings, and the module
header in the right pane. The artboard shows neither — but it also shows only 15
of 34 rows, so the frame may simply be abbreviated. Nothing is blocked on them.

`mockups/dark-mode.html` is deliberately untracked (peer session's, and dark mode
was explored as a mockup only). A peer session was live in this repo this evening
and journals into the same memory tree.
