# Handoff — 3 September 2026

If you (or a fresh session) opens this next, here is where we are.

Four commits, `38c7943 → b82e146`, **all deployed and hash-verified** against
production (92 of 92 servable files identical). The home page is now Figma node
**135:4436** — seventeen cards in five rows, no workspace tabs, Audit added,
Housing inside the grid. Before that: the two Global Search sheets widened for
tablet/desktop, a design review applied in full (focus trapping, 44px targets,
ARIA, contrast), and a 4/8pt spacing audit of the search surface. The single
most important thing to know: **four rounds of "it still doesn't match" were all
one root cause** — a saved layout the `VERSION` gate never invalidated, which a
fresh-profile harness cannot see. `VERSION` is now 6, so returning browsers drop
their saved arrangement. Next move is the user's call on the open questions
below; nothing is blocked.

---

## How to accept what shipped

| Try this | You should see | If not |
|---|---|---|
| Open the live URL | 17 cards in 5 rows: Medical · Species(2×1) · Hospital(2×1) on top; Audit at row 2 col 5; no tabs above the greeting | Hospital at cols 2–3 with Species 2×2 means the `VERSION` bump did not land — check `antz.layoutVersion()` returns 6 |
| Look at Reports, Pharmacy, Users, Pending Approvals | Smooth gradients, corner to corner | A hard diagonal seam means a two-layer wash came back; they must be single sampled layers |
| Compare Hospital and Diet & Kitchen | Both bright photos, dark band only across the bottom ~70px; Hospital slightly darker overall | A band covering two thirds of the card means `--scrim-band-h` reverted to 113px |
| Tap **Housing**, then the first breadcrumb | It reads **Home** and returns to the Command Centre | Reading "Housing" means the relabel did not land — and it is the only labelled way home now |
| Open Global Search → **Select Tags** at ≥1024px | A 960px sheet, scope control and search field on one row | A 460px sheet means the responsive block did not land |
| In that sheet, press **Escape** | It closes and focus returns to the search field | Nothing happening means focus is not being put into the sheet |
| Watch the search field's placeholder | Cycles Animals · Users · Site · Section · Enclosure · Identifier | Modules/Widgets means `SEARCH_WORDS.modules` reverted |

The spacing audit (`3156c26`) has no single visual tell — it moved the search
surface onto a 4/8pt scale and left six values off-grid on purpose (five
tap-target `::after` insets, one optical 42px).

---

## Open, and all the user's to decide

1. **The two round search-row buttons.** The node draws them without saying what
   they do. Wired to the nearest existing capability: scan → Global Search,
   grid → Add Module. Neither invents a scanner.
2. **`Search "Site"` / `"Section"` are singular** while `"Animals"` is plural —
   the brief's own wording, used verbatim. One word to pluralise.
3. **Lab's card name is 20px in the artboard** where the other thirteen are 18.
   Treated as a slip; set to 18 everywhere.
4. **The Appearance/theme toggle was removed** — node 135:4442 holds exactly two
   controls. Dark mode only ever existed as `mockups/dark-mode.html`.
5. **Nothing comes out of an enclosure in one press any more** except the Home
   crumb; the removed tab used to.
6. **`_planning/handoff_2026-09-02.md` is still untracked** and its predecessor
   is tracked. Raised four times without a ruling.
7. **Five older rulings from the 2 Sep note remain open**: §15 tonal gradients,
   whether action widgets belong on the Site Workspace, the §3/§30 deletions,
   and the Add Module rail headings + right-pane module header.
8. **Four animation suggestions, none built.** Highest leverage is the
   `dashboard.html` entrance — it never inherited `landCards()`, so 9–10 cards
   teleport in on a surface whose siblings all bridge.
9. **Card-layout spacing outside `.gsx*` still drifts** (`gap: 5px` ×11,
   `gap: 7px`, `padding: 2px 8px`). The audit covered the search surface only.
10. **A static server may still be listening** on `127.0.0.1:8000` (PID 2486).

Cost that shipped knowingly: the `VERSION` bump discards any custom home-page
arrangement a user had saved. That was the only way a returning browser could
pick up the artboard.

---

# Handoff — 3 September 2026, evening session

If you (or a fresh session) opens this next, here is where we are.

Ten commits `162d374 → f8883c6`, **all deployed to antz-module-selection-home.vercel.app,
hash-verified, and pushed to `origin/site-command-centre`** (`main` is behind and
fast-forwardable; not touched). The home page now reads: announcement deck →
Modules header → Command Centre banner at module-row height → 16-card grid
(Follow Up hidden, `VERSION` 7). The deck is the designer's card from Figma frame
**152:4159** (node 159:3912: white 4px-inset card, gradient-tinted panel, gradient
title, photo right, like button; Important variant red/orange, no photo), stacked
Hotstar-style (.909 @ +4px, .795 @ +10px), turning on swipe / arrows / a six-second
dwell that pauses on hover, keyboard focus, a held finger, or a hidden tab. The
Add Module rail is one flat list with pin + drag (no category headings). Pending
Approvals draws the Figma clock. The Modules button beside search is gone; Edit
in the header is the way into Add Module. A mockup of the banner options lives
locally only (`mockups/home-banners.html`, untracked, never deployed).

**Verify it landed:** open the live URL at 1024 → the first band is the wild-dog
announcement with the lion photo on the RIGHT and two card edges peeking behind it
on the right; wait 6s → the red "Important" card slides in; under it "Modules …
▦ Edit" in green, then the estate banner at the same height as Medical. If the
photo is on the left or dots show under it, the page is cached — hard reload.

**Open, all the user's to decide:**
1. **Reports.** Frame 152:4159's Modules group is fixed at 947px and clips row 5,
   so Reports is invisible in the design. Left on the page; needs a yes/no.
2. **Security glyph** — Figma draws a shield, app a padlock. Not raised as a task.
3. Six untracked files (`_planning/handoff_2026-09-0{2,3}.md`, `mockups/dark-mode.html`,
   `mockups/home-banners.html`, `mockups/Announcement & Notes/`, `assets/icon.zip`).
   The deploy step moves the banner mockup + photos aside so they never upload.
4. A local static server is listening on `127.0.0.1:8000` (PID 1862).

Verification scripts for the deck live in the session scratchpad
(`verify_figma.py`, `verify_deck.py`), not in `tools/` — recreate from `tools/verify.py`
+ `tools/cdp.py` if needed; `tools/verify.py` itself passes.
