# Handoff — 2026-09-10

If you (or a fresh session) opens this next, here's where we are.

Three commits landed — the cdp.py profile-leak fix (`06a9dae`), the module
launcher (`8701d67`), then white ink on all nineteen tiles, a light-black page
backdrop and the removal of the per-tile fan in favour of one 220ms panel scale
(`cd20968`). **Everything after that is uncommitted**, and it is the larger
half: node `321:3570` implemented (the 744 artboard — the phone/tablet media
boundary moved 767/768 → 743/744, which makes 162px columns and 144px cards
fall out of the existing `cqw` derivation rather than being pinned), tabular
figures, pager-dot hit areas, the chat disc held still through the pill's
collapse, the module surface extended to the page foot, the foliage feather,
a `ki-glow` pulse on Key Insights, and the profile docking into the sticky
search bar (`createProfileMenu` now takes an array of anchors). Suite is ALL
PASS at 1024 and 768. **The live alias is still on `8701d67`, four rulings
behind** — so the deploy is the single most important next move, after a commit
that must include the untracked `assets/img/foliage-artboard.png` (the CSS
references it; without it the header has no artwork) and
`mockups/header-scroll.html`.

Two things still unruled: where the sixteen Quick Actions verbs live now that
the pill opens modules (`QA_CONTENT` at index.html:29360 is a one-word revert,
and it decides whether a tile NAVIGATES or EXPANDS — do not build on the
launcher until it is settled), and the landscape fold, where only 3 of 17 cards
are visible at 1024×768 because every lever is a recorded ruling.
